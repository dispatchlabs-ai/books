Use case: precise-object-edit. Edit target: supplied paired desktop/mobile Books UI board. Preserve layout and all unaffected content. Apply precisely: Keep layout. Replace signed amount column value Auto-detect with 'Amount'; replace status mapping Use default mapping with 'Status → Booked / Pending'. These are explicit reviewed CSV column choices, not unimplemented automatic parsing. Label Save as profile 'Save on this device'. Add compact 'Open job by ID' field/link to recent uploads. Failed parsing needs corrected options and a new upload when source invalid; menu can offer diagnostics. No Cancel import action.
