Use case: precise-object-edit. Edit target: supplied paired desktop/mobile Books UI board. Preserve layout and all unaffected content. Apply precisely: Preserve comparison design but existing candidate must be 'Existing source movement', source 'Prior statement import', with source ID SRC-20260314-0451; never a manual journal entry, because identity matching compares source movements. Both amounts are -$75.00. Keep one decision remaining by leaving both radios unselected and reason field empty with placeholder 'Explain your choice'. Replace Cancel import with 'Back to mapping' because no server cancellation exists. Preview stays disabled until selected+reason.
