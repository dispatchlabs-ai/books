Use case: precise-object-edit. Edit target: the supplied paired desktop/mobile Books UI board. Preserve its layout, design language and all unaffected content. Apply these corrections accurately in both viewports: Correct only the Source coverage graphic in both viewports: it is a 24-movement total with exactly two segments, 20 posted and 4 source-only. Remove the extra gray segment; display 24 imported as a text total outside the two-segment legend. Keep all other layout and content.
