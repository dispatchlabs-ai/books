Use case: precise-object-edit. Edit target: the supplied paired desktop/mobile Books UI board. Preserve its layout, design language and all unaffected content. Apply these corrections accurately in both viewports: Keep all layout and figures. Under Undo posting, replace the incorrect 'Move to draft (if allowed)' with 'Create a linked reversal'. Posted originals NEVER move back to draft. Change actor name Example Studio to 'Demo operator' in both viewports to avoid confusing entity with actor. Keep original immutable and balanced lines.
