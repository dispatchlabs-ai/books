Use case: precise-object-edit. Edit target: supplied Books desktop/mobile board. Preserve layout but correct Schema version to '2' everywhere, not a date. Preview migration description 'Review required schema changes for the selected database.' Initialize target must be disabled for current existing Example Studio database, helper 'Select an authorized missing target to initialize'. Sidebar entity labels consistently Example Studio. MOBILE show restore confirmation sheet with Preview verified, field 'example-studio' and Restore button, so recovery is fully designed. Keep backup/diagnostics navigation behind sheet.
